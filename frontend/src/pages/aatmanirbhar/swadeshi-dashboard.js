import Head from 'next/head';
import Header from '../../components/common/Header';
import Footer from '../../components/common/Footer';
import StatCard from '../../components/common/StatCard';

const startupData = [
  { year: '2019', count: 38756 },
  { year: '2020', count: 41061 },
  { year: '2021', count: 55816 },
  { year: '2022', count: 72993 },
  { year: '2023', count: 90000 },
];

const localVsImport = [
  { category: 'Electronics', local: 45, imported: 55 },
  { category: 'Textiles', local: 78, imported: 22 },
  { category: 'Food & Beverages', local: 92, imported: 8 },
  { category: 'Automobiles', local: 65, imported: 35 },
  { category: 'Pharma', local: 70, imported: 30 },
];

export default function SwadeshiDashboard() {
  const maxStartups = Math.max(...startupData.map(d => d.count));

  return (
    <>
      <Head><title>Swadeshi Dashboard | Aatmanirbhar Bharat</title></Head>
      <div className="min-h-screen flex flex-col bg-gray-50">
        <Header />
        <main className="flex-1 max-w-7xl mx-auto w-full px-4 py-8">
          {/* Hero */}
          <div className="bg-gradient-to-r from-india-green to-green-600 rounded-2xl p-8 mb-8 text-white">
            <h1 className="text-3xl font-bold mb-2">📈 Swadeshi Dashboard</h1>
            <p className="text-green-100 text-lg">Tracking the growth of indigenous products, startups, and employment</p>
          </div>

          {/* Stats */}
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <StatCard icon="🏭" value="90,000+" label="DPIIT Startups" color="saffron" trend="23% YoY growth" />
            <StatCard icon="💼" value="11.4 Cr" label="Jobs Created" color="green" trend="8.2% increase" />
            <StatCard icon="💰" value="₹15.7L Cr" label="MSME Output" color="navy" trend="12% growth" />
            <StatCard icon="🌍" value="₹4.5L Cr" label="MSME Exports" color="saffron" trend="15% increase" />
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
            {/* Startup Growth Chart */}
            <div className="bg-white rounded-xl shadow-md p-6">
              <h2 className="text-lg font-bold text-navy mb-6">🚀 Startup India - DPIIT Recognized Startups</h2>
              <div className="flex items-end gap-4 h-48">
                {startupData.map((item) => {
                  const height = Math.round((item.count / maxStartups) * 100);
                  return (
                    <div key={item.year} className="flex-1 flex flex-col items-center gap-1">
                      <span className="text-xs text-gray-500 font-medium">{(item.count / 1000).toFixed(0)}K</span>
                      <div className="w-full bg-india-green rounded-t-sm transition-all hover:bg-green-600" style={{ height: `${height}%` }} />
                      <span className="text-xs text-gray-500 font-medium">{item.year}</span>
                    </div>
                  );
                })}
              </div>
            </div>

            {/* Local vs Import */}
            <div className="bg-white rounded-xl shadow-md p-6">
              <h2 className="text-lg font-bold text-navy mb-6">🇮🇳 Local Production vs Imports</h2>
              <div className="space-y-4">
                {localVsImport.map((item) => (
                  <div key={item.category}>
                    <div className="flex justify-between text-sm mb-1">
                      <span className="text-gray-700 font-medium">{item.category}</span>
                      <span className="text-india-green font-semibold">{item.local}% Local</span>
                    </div>
                    <div className="w-full h-3 bg-gray-100 rounded-full overflow-hidden flex">
                      <div className="h-full bg-india-green transition-all" style={{ width: `${item.local}%` }} />
                      <div className="h-full bg-red-300 transition-all" style={{ width: `${item.imported}%` }} />
                    </div>
                    <div className="flex justify-between text-xs text-gray-400 mt-0.5">
                      <span>🟢 Local: {item.local}%</span>
                      <span>🔴 Import: {item.imported}%</span>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>

          {/* Employment Generated */}
          <div className="bg-white rounded-xl shadow-md p-6">
            <h2 className="text-lg font-bold text-navy mb-6">👷 Employment Generated by Sector (Lakhs)</h2>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
              {[
                { sector: 'Manufacturing', jobs: 485, color: '#FF9933' },
                { sector: 'Services', jobs: 320, color: '#138808' },
                { sector: 'Agriculture', jobs: 220, color: '#8B4513' },
                { sector: 'Technology', jobs: 115, color: '#000080' },
              ].map((item) => (
                <div key={item.sector} className="text-center p-4 rounded-xl" style={{ backgroundColor: item.color + '15', borderTop: `3px solid ${item.color}` }}>
                  <div className="text-3xl font-bold mb-1" style={{ color: item.color }}>{item.jobs}L</div>
                  <div className="text-sm text-gray-600">{item.sector}</div>
                </div>
              ))}
            </div>
          </div>
        </main>
        <Footer />
      </div>
    </>
  );
}
